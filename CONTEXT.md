# Context

本文件是本项目的**统一词汇表**。讨论、设计、实现时使用这里的术语，避免同义词混用。

项目性质：本地素材库的语义检索服务。对个人素材库提供「文字找画面」与「图片找画面」两种检索方式，检索结果指回素材原文件位置。

---

## 领域术语

### 素材

**一条视频文件**。检索结果的最小业务单位。素材包含多个画面。

注意：系统内部另有「帧」这一更细的单位，但**面向用户的概念是素材**。

### 帧

素材中的一个画面时刻，由素材按固定时间间隔抽取得到。每个帧都有一张预览图和一个时间戳。帧是**向量索引的存储单位**，但不是**用户认知的检索单位**。

### 帧级 vs 视频级（检索粒度）

- **帧级**：检索结果每行是一个帧。同一素材的多个相似帧会占满多行。
- **视频级**：检索结果每行是一个素材，同一素材只出现一次，附带其最具代表性的一帧。

当前系统：语义检索为帧级；本项目将其收敛为视频级。

### 召回（通道）

一次检索中独立产生候选结果的一条检索路径。检索最终结果是**多个召回通道的融合**。

| 通道 | 依据 | 能覆盖 |
|---|---|---|
| **语义通道** | 查询文本与画面特征的向量相似度 | "画面里有什么" |
| **关键词通道** | 查询字符串在素材文字信息上的子串匹配 | "素材叫什么" |

两个通道**互补，不可互相替代**。

### 语义通道

原有的检索通道。把查询文本编码为向量，与帧向量做近邻检索。擅长描述性查询（"雨天跑步""面料特写"），**对专有名称无能**——因为专有名称通常不出现在画面里，且通用视觉模型不认识这些词。

### 关键词通道

本项目新增的通道。依据素材的**文字信息**（文件名 + 有意义的父目录名）做子串匹配。擅长专有名称查询，对描述性查询无能为力。

### 素材文字信息

参与关键词通道匹配的文本集合，由两部分构成：

- **文件名**（去扩展名）
- **有意义的父目录名**——路径拆成目录段后，剔除纯数字段与哈希段，只保留可读段

完整路径字符串**不参与**匹配：它会带入无语义的中间层级，制造噪声。

### 类型化文件名

形如 `OUT_0601`、`260714_C0500` 的文件名：由日期码或工单码构成，不含自然语言含义。这类文件名**只能靠精确相等命中**，不做子串匹配——否则短数字片段会在其它文本中巧合误伤。

### 视频级文本索引

关键词通道使用的内存结构：素材 → 可匹配文本。服务启动时从向量库元数据一次性构建，**不落盘、不建第二份持久化存储**，因此不存在两份数据不一致的运维负担。

规模前提：素材量在千级时，内存子串扫描是亚毫秒级。量级增长到十万级时应替换为倒排索引。

### 真实失败形状回归集

用于防止真实专有名称失败模式回归的可提交测试集合。每条用例只保留失败形状，不保存真实素材名称：

- `proper_name_unmapped`：专有名称没有可靠语义映射；
- `filename_not_exact`：查询是文件名的简称或口头叫法；
- `parent_only`：查询只出现在有意义的父目录片段；
- `generic_overlap`：通用词与热门目录片段重合，目标容易被其它素材挤掉。

回归集中的查询、目标 ID、文件名和目录片段均为中性占位值，并绑定确定性的内存测试夹具。它与从元数据自动生成的自验证样本不同：自验证样本证明「可匹配文本可以召回」，失败形状回归集证明「关闭关键词通道时应当无法召回，开启后应当召回」。评测门禁默认执行这组失败形状检查；关闭关键词通道的负对照应返回非零退出码。

### 融合（排序）

把多个召回通道的结果合并为最终排名的过程。

**倒数排名融合（RRF）**：某条结果在某通道排第 `rank` 名，贡献 `1 / (60 + rank)` 分；各通道贡献分相加，降序为最终排名。

选用理由：各通道分数**量纲不可比**（语义通道是余弦相似度 0~1，关键词通道是"有/无"），任何加权求和都需要额外的归一化规则与权重调参，且随模型更换而失效。RRF 只看名次，天然免疫此问题。

### 命中来源（match_type）

标注某条结果由哪些通道召回：`semantic` / `keyword` / `both`。用于让用户理解"这条为什么排前面"，供前端渲染提示。

### 特性开关

服务端配置项，可关闭关键词通道使检索退回纯语义行为。用于融合效果不理想时快速回退，**改配置重启即可，无需改代码重新发版**。

---

## 刻意排除的概念

以下概念在本项目中**不存在**，讨论时不要引入：

- **标签字典 / 人工标签体系**——本项目不做人工打标。素材的可检索信息来自自动抽取（画面向量 + 文件名文字），不来自人工标签。
- **知识图谱**——与本项目无关，属另一条工作线。
- **分词 / 中文分词器**——关键词通道采用子串匹配，不需要分词。
- **同义词表 / 别名表**——刻意不做，维护成本高且易过期；这类需求由以图搜图覆盖。

---

## 相关文档

- 架构与 API 契约：仓库根 `README.md`
- 环境与站点配置：仓库外 `~/.config/video-finder/site.json`（不进仓库）
